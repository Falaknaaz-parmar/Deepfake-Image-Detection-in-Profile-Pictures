# Vercel Deployment Requirements (Lightweight - No ML Libraries)
# For local training, use requirements-local.txt instead

# Web Framework
Flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.0

# Image Processing
Pillow==10.0.0

# Note: PyTorch and ML libraries excluded for Vercel deployment
# This allows demo mode to work without hitting Vercel's size limits
# For local development with full model, use: pip install -r requirements-local.txt
